<input
	pattern="{{pattern}}"
	name="{{id}}"
	type="{{type}}"
	id="{{id}}"
	class="{{class}}"
	type="text"
	maxlength="{{maxLength}}"
	minlength="{{minLength}}"
	required="{{required}}"
	placeholder="{{placeholder}}"
>
